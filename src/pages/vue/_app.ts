//在此处配置vue 全局变量和添加插件   https://docs.astro.build/zh-cn/guides/integrations-guide/vue/
import type { App } from 'vue';

export default (app: App) => {
  //app.use(xxx);
  //app.config.globalProperties.msg = 'bbzywkq'
};